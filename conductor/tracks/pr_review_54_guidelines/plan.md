# プラン: PR #54 レビュー指摘事項の修正

PRレビューでの指摘に基づき、ガイドラインおよび設定ファイルを修正する。

## 目的
- ガイドライン文書内の不整合を解消する。
- `.editorconfig` の設定漏れを修正する。
- 存在しないディレクトリへの参照や、不要なテストセクションを整理する。

## タスクリスト
- [ ] **`conductor/github_issue.md` の修正**: `type` 一覧に欠落している `style` を追加する。
- [ ] **`conductor/code_styleguides/rust_win32.md` の修正**:
    - [ ] `4.2 統合テスト (Integration Tests)` セクションを削除する（ユーザー判断）。
    - [ ] Layer 1 Domain のテスト目標を「カバレッジ 90% 以上」に統一し、記述の矛盾を解消する。
- [ ] **`.editorconfig` の修正**: `[*.ps1]` セクションを追加し、インデント設定を明示する。
- [ ] **`AGENTS.md` の修正**: `sdk/` の参照を EmEditor SDK 公式サイトの URL に更新する。
