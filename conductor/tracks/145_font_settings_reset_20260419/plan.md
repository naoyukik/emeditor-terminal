# 実装計画 (Implementation Plan): リサイズ・起動時のフォント設定リセット問題の修正

## フェーズ 1: 問題の把握と詳細設計 (Discovery & Detailed Design)
- [ ] Task: `autonomous-researcher` スキルを使用し、ターミナルプラグインのリサイズ (`on_size`) および起動時におけるフォント設定（サイズ、ウェイト等）のロード処理を調査し、`evidence_report.md` を作成する。
    - [ ] ターミナル起動時の設定ロードフローを特定する。
    - [ ] `on_size` イベントハンドラ内での設定ロード/更新処理を特定する。
    - [ ] なぜ `font_size=0` や `weight=0` となるのか、根本原因（例：EmEditor本体のAPI呼び出し失敗、ハンドルの取得ミスなど）を解析する。
- [ ] Task: 調査結果に基づき、本 `plan.md` の後続タスク（修正箇所、バリデーションロジックの追加等）を「どのファイルのどの行をどう変えるか」レベルまで具体的に書き換える。
- [ ] Task: Conductor - ユーザー手動検証 'フェーズ 1' (調査結果と具体的プランの承認)

## フェーズ 2: フォント設定ロード処理の修正とバリデーションの実装 (Implementation & Validation)
- [ ] Task: フェーズ 1 の調査結果に基づき、不正な値（`0`など）がロードされた場合のバリデーション層、またはデフォルト値へのフォールバックロジックを実装する。
    - [ ] 該当ファイルの修正（詳細はフェーズ1で決定）。
    - [ ] 関連するユニットテストの追加・修正（カバレッジ80%以上を目標）。
- [ ] Task: 修正内容に対するコードのフォーマットと静的解析を実行する。
    - [ ] `cargo clippy` を実行し、警告を解消する。
    - [ ] `cargo fmt` を実行する。
- [ ] Task: `AGENTS.md` の規約 (Conventional Commits) に従い、フェーズ 2 の成果物をコミットする。
- [ ] Task: エディタを起動・リサイズし、フォント設定が維持されていること（ログおよび実際の描画）を確認する手動テストを実施する。
- [ ] Task: Conductor - ユーザー手動検証 'フェーズ 2' (実装内容の動作確認と承認)

## フェーズ 3: 最終レビューとクリーンアップ (Final Review & Cleanup)
- [ ] Task: `architecture-validator` スキル（または該当ツール）を実行し、アーキテクチャの依存関係に違反がないか確認する。
- [ ] Task: Issueのクローズ条件（Acceptance Criteria）が全て満たされているか最終確認する。
- [ ] Task: Conductor - ユーザー手動検証 'フェーズ 3' (全タスク完了の承認)